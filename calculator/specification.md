
ok specification time - 

```python

def tokenize():
	""" takes a string, with a certain expression, and turns them into
a list of tokens while respecting joint characters (2.5, -1)"""

def parse():
"""takes a list of TOKENS (strings) and turns them into nested expressions (what
data structure should we use  for this?) 

def evaluate():
""" takes a data structure, turns them into actual function objects we know how
to differentiate.""" 


